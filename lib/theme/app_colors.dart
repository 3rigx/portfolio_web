// Define theme colors for dark mode with mint green accent
import 'package:flutter/material.dart';

final class AppColors {
  static const background = Color(0xFF0A0A0A);
  static const primary = Color(0xFF00FF9D);
  static const primaryDark = Color(0xFF00CC7D);
  static const surface = Color(0xFF111111);
  static const text = Color(0xFFFFFFFF);
}

// Ask Copilot to help with ThemeData implementation using these colors